return   {
    "dressing.nvim",
    opts = {
      input = {
        -- When true, <Esc> will close the modal - Defaults to true
        insert_only = false,  -- vim motion in modal
      },
    },
  }
