---
name: grill-me
description: A relentless interview to sharpen a plan or design.
disable-model-invocation: true
---

Before starting, ask whether to use `/grill-with-docs` instead (it also produces ADRs and a glossary via `/domain-modeling` as we go). If yes, run `/grill-with-docs` skill instead. Otherwise, run a `/grilling` session.
